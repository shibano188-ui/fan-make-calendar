# Fan Make Calendar — 投稿促進・報酬システム 機能仕様書
# Claude Code 実装指示書

## 背景・目的

現状のFan Make Calendarはカレンダー機能として完成しているが、
「情報が揃いやすい状態」を作るための投稿動機づけが不十分。

本仕様は以下を解決する：
- コールドスタート問題（誰も投稿しない状態）
- TimeTree・oshimoaとの差別化
- Twitterで作品情報を発信しているユーザーの動機をアプリ内に移植する

ターゲット投稿者像：
- Xで作品情報を発信している人
- 自分の集めた情報をみんなのために役立てたい
- 反応・承認欲求を満たしたい
- できれば収益につなげたい（Xの収益化は難しいため移植先を求めている）

---

## 1. リアクションシステム

### 1-A. いいねの拡張（絵文字スタンプ）
現在の「いいね（♡）」に加え、mixi2スタイルの複数リアクションを追加する。

**実装するリアクション種類（第一弾）：**
| 絵文字 | ラベル | 用途 |
|--------|--------|------|
| ♡ | いいね | 基本リアクション（既存） |
| 📅 | 行きたい！ | イベント参加意思表示 |
| 🔥 | アツい！ | 盛り上がり系 |
| 🎉 | すごい！ | 驚き・称賛 |
| 👑 | 最高！ | 最上級の称賛 |

**UI仕様：**
- イベントカードの♡ボタンを長押し or タップ→絵文字ピッカーが展開
- 各リアクションの合計数を小さく表示（例: ♡ 234 🔥 12）
- ポイント計算にはリアクション種類ごとに重みをつける
  - いいね: 1pt、行きたい: 2pt、アツい: 2pt、すごい: 3pt、最高: 5pt

**Supabaseスキーマ変更：**
```sql
-- likesテーブルにreaction_typeカラムを追加
ALTER TABLE likes ADD COLUMN IF NOT EXISTS reaction_type TEXT DEFAULT 'like';
-- reaction_type: 'like' | 'want' | 'hot' | 'amazing' | 'best'
```

**将来拡張（実装不要、設計考慮のみ）：**
- 追加スタンプパック（有料購入）
- 季節限定スタンプ
- ※いいね数との共存は別途UX設計が必要なため、現段階では合算せずそれぞれ独立表示

---

## 2. ポイント・報酬システム

### 2-A. ポイント付与ルール

**投稿者へのポイント付与：**
| アクション | ポイント |
|-----------|---------|
| 新規イベント投稿（承認後） | +10pt |
| もらったいいね | +1pt/件 |
| もらった行きたい | +2pt/件 |
| もらったアツい | +2pt/件 |
| もらったすごい | +3pt/件 |
| もらった最高 | +5pt/件 |
| 重複投稿への修正提案が採用された | +5pt |
| 重複投稿者（2番手以降）へのいいね | +0.5pt/件（1番手の50%） |
| 新規作品カレンダーを初めて開拓した | +50pt（ボーナス） |
| 月間投稿数ランキング上位（1位） | +100pt |
| 月間投稿数ランキング上位（2位） | +50pt |
| 月間投稿数ランキング上位（3位） | +30pt |

**ズル対策（必須）：**
- 同一ユーザーが自分の投稿にいいねできないようにRLSで制御（既存）
- 同一IPアドレスからの複数アカウント作成を検知してフラグ立て
- 投稿後24時間以内のいいねは「新着ボーナス」として扱い、異常に多い場合は審査対象に
- ポイント付与は即時ではなく24時間後に確定（取り消し可能な猶予期間）
- 同一作品への同一日付・同一タイトルの投稿は重複として自動検知

**Supabaseスキーマ（新規テーブル）：**
```sql
CREATE TABLE IF NOT EXISTS user_points (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id),
  points INTEGER DEFAULT 0,
  total_earned INTEGER DEFAULT 0, -- 累計獲得ポイント（ステータス用）
  created_at TIMESTAMPTZ DEFAULT now(),
  updated_at TIMESTAMPTZ DEFAULT now()
);

CREATE TABLE IF NOT EXISTS point_transactions (
  id UUID PRIMARY KEY DEFAULT gen_random_uuid(),
  user_id UUID REFERENCES auth.users(id),
  amount INTEGER NOT NULL, -- 正=獲得、負=消費
  reason TEXT NOT NULL, -- 'like_received' | 'post_created' | 'gift_exchange' など
  reference_id UUID, -- 関連するevent_id or like_idなど
  status TEXT DEFAULT 'pending', -- 'pending' | 'confirmed' | 'cancelled'
  confirmed_at TIMESTAMPTZ,
  created_at TIMESTAMPTZ DEFAULT now()
);
```

### 2-B. ギフトカード交換

**交換レート（暫定）：**
| ポイント | 交換先 |
|---------|--------|
| 500pt | Amazonギフトカード 500円分 |
| 1000pt | Amazonギフトカード 1000円分 |
| 500pt | App Store & Google Playギフトカード 500円分 |

**UI：**
- 設定画面またはプロフィール画面に「ポイント交換」セクションを追加
- 現在のポイント残高を大きく表示
- 交換ボタン → 確認モーダル → 申請完了（メール通知）
- ⚠️ 初期実装では「申請フォーム送信」のみ。実際の交換処理は手動運用でOK

**注意（実装時に考慮）：**
- 景品表示法・資金決済法の観点から、ポイントの有効期限（例：獲得から1年）を設ける
- 交換申請にはメールアドレスの確認を必須にする

---

## 3. ステータス・称号システム（デスストランディング風）

### 3-A. 累計ポイントによる称号

いいね数・累計獲得ポイントによって表示名の横に称号バッジが付く。

| 累計ポイント | 称号 | バッジ色 |
|------------|------|---------|
| 0〜99 | 見習いファン | グレー |
| 100〜499 | ファン | ブルー |
| 500〜1499 | 熱心なファン | グリーン |
| 1500〜4999 | 伝道師 | パープル |
| 5000〜9999 | レジェンド | ゴールド |
| 10000〜 | 神 | レインボー（アニメーション） |

**UI：**
- イベントカードの投稿者名横に称号バッジを小さく表示
- プロフィール画面に現在の称号を大きく表示
- 称号が上がった時はアニメーション演出（紙吹雪など）

### 3-B. 特殊称号（実績バッジ）

累計ポイントとは別に、特定の行動で獲得できる実績バッジ。

| 実績 | 称号 |
|------|------|
| 初めて投稿した | 🌱 はじめの一歩 |
| 100件投稿した | 📚 百投の達人 |
| 新規作品を開拓した | 🗺️ 開拓者 |
| いいね累計1000件もらった | ❤️ いいね王 |
| 1つの作品に50件投稿した | 🏆 作品マスター |
| 修正提案が10回採用された | 🔧 情報職人 |

### 3-C. プロフィール画面

既存の設定画面を拡張してプロフィール画面を作る。

**表示内容：**
- 表示名・ホーム県（既存）
- 現在の称号（大きく表示）
- 累計ポイント・現在の保有ポイント
- 獲得した実績バッジ一覧
- 投稿数・もらったいいね総数
- 次の称号まであと何ポイントかのプログレスバー

---

## 4. 入力補助（AI自動抽出）

### 4-A. URLから自動入力
投稿フォームにURLを貼り付けると、AIがページを解析してタイトル・日付・時間・場所を自動入力する。

**実装方針：**
- Anthropic API（claude-sonnet-4-20250514）を使用
- フロント側でURLを受け取り、Supabase Edge Functionでページ取得→AI解析
- レスポンスをフォームの各フィールドに自動セット（ユーザーが確認・修正可能）

**プロンプト例：**
```
以下のWebページの内容から、イベント情報を抽出してください。
JSON形式で返してください。
{
  "title": "イベントタイトル",
  "date": "YYYY-MM-DD",
  "time": "HH:MM",
  "category": "単行本|グッズ|イベント|誕生日|配信|その他",
  "location": "場所名",
  "prefecture": "都道府県",
  "link": "公式URL",
  "memo": "補足情報"
}
ページ内容: {{page_content}}
```

### 4-B. スクリーンショットから自動入力
画像をアップロードするとAIがOCR+解析してフォームに自動入力する。

**実装方針：**
- Anthropic APIのvision機能を使用（base64画像を送信）
- 4-Aと同じJSON形式でレスポンスを受け取る

**UI：**
- 投稿フォームの上部に「URLを貼る」「画像をアップロード」ボタンを追加
- 解析中はスケルトンローディング表示
- 解析完了後、各フィールドにプリフィルされた状態でユーザーに確認させる

---

## 5. 実装優先順位

以下の順番で実装すること。

**Phase 1（最優先）：リアクション拡張**
- 複数リアクション種類の追加（Supabaseスキーマ変更含む）
- リアクションピッカーUI

**Phase 2：ポイントシステム基盤**
- user_points・point_transactionsテーブル作成
- ポイント付与ロジック（投稿・いいね受信時）
- プロフィール画面のポイント表示

**Phase 3：称号・実績システム**
- 累計ポイントによる称号計算ロジック
- 称号バッジUI（イベントカード・プロフィール画面）
- 実績バッジ

**Phase 4：ギフトカード交換**
- 交換申請フォーム（初期は手動運用）

**Phase 5：AI入力補助**
- URLからの自動入力
- スクリーンショットからの自動入力

---

## 6. 既存実装との整合性注意事項

- `src/types.ts` の `CalendarEvent` 型に `reaction_counts` フィールドを追加
- `src/lib/api.ts` のいいね関連関数を拡張（reaction_type対応）
- `src/pages/DateDetail.tsx` のいいねボタンをリアクションピッカーに差し替え
- 既存の `likes` テーブルのデータは `reaction_type = 'like'` として移行
- ⚠️ 既存機能（いいねアニメーション・赤グロー・バネアニメ）は必ず保持すること
- ⚠️ iOS Safariのスクロール対策（絶対配置方式）は変更しないこと
- ⚠️ z-index階層（BottomTab: z-[100]、Sheet類: z-[200]）を維持すること

