# 提出前の審査基準チェック（2026-08-20・ビルド6）

5回却下されているので、App Review Guidelines のうちこのアプリに当たる条項を一通り見た。
「確認した」は**コードか実機で根拠を取った**もの、「本人確認」は App Store Connect 側でしか見られないもの。

---

## 1. このセッションで直したもの

| 条項 | 問題 | 対処 |
| --- | --- | --- |
| 2.1 | `<input type="file" accept="image/*">` の Take Photo でTCCクラッシュ | カメラ経路を廃止（`docs/app-store-review-2026-08-20-reply.md`） |
| 2.1 | 利用規約/プライバシーポリシーを開くと**戻る手段が最下部の「FanHive を開く」だけ**。押すとアプリがホームから起動し直しで、購入画面に戻れない | 3ページの上部に「← 戻る」(history.back)を追加。safe-areaの余白とダークモードも |
| 2.1 | 原寸の写真を data URL で保存すると localStorage を超えて**アプリごと白画面** | 幅1600/画質80に制限＋保存を try/catch |
| 1.2 | アプリ内に規約への同意点が無かった | 投稿ボタンの下に「投稿すると利用規約に同意したものとみなされます／不適切な投稿は削除され、繰り返した場合は利用を停止します」 |

規約ページの件は、審査員が 3.1.2 の確認で**必ず**踏む導線だった。前回の却下が
「ボタンが無反応」なので、同じ読まれ方をされる可能性が高かった。

## 2. 満たしていることを確認したもの

### 2.1 クラッシュ・完成度

- 他に用途説明が要るAPIを触っていない。位置情報・マイク・連絡先・モーション・音声認識は
  コードに一切出てこない（`geolocation` / `mediaDevices` / `SpeechRecognition` を全文検索）
- カレンダープラグインの `checkAllPermissions` は `EKEventStore.authorizationStatus(for:)` を
  読むだけで、リマインダーの許可要求はしない（プラグインのSwiftを読んで確認）。
  → `NSRemindersFullAccessUsageDescription` は不要
- RevenueCat の iOSキーがバンドルに入っている。**入っていないと購入画面が
  「ただいまお支払いの準備中です」になり、2.1で即落ちる**
- 「準備中…」の表示はICS URLを作る一瞬だけ（プレミアムの折りたたみの中）
- `?demo=1` はオンボーディングの体験用で、画面に「投稿（例）」「今は投稿を押しても
  登録はされません」と出る。隠し機能(2.3.1)ではない

### 3.1.2 定期購読（購入画面で実機確認）

購入ボタンと同じ画面に、価格 `¥500/月`、`最初の1か月は無料。以降は月¥500で自動更新されます。
いつでも解約できます。`、**利用規約 / プライバシーポリシー / 購入を復元** が並んでいる。
別画面送りにしていない。

### 5.1.1 データ収集

- 会員登録なしでアプリのすべての機能が使える（匿名アカウントを自動作成）
- 購入に会員登録を要求しない。購入画面に登録の入口を置いていない（8/17に修正済み）
- アカウント削除がマイページの設定リスト最終行にある。2段階で、
  「投稿・いいね・保存した予定を含むすべてのデータが削除されます」と明示

### 1.2 ユーザー生成コンテンツ

| 要求 | 状態 |
| --- | --- |
| 不適切な投稿の通報 | 投稿の詳細から通報。以後その人には表示されない |
| 迷惑ユーザーのブロック | プロフィールからブロック。マイページで解除できる |
| 通報への速やかな対応 | 規約に「24時間以内に削除または利用停止」と明記 |
| 連絡先の公開 | `about.html` に運営者名と `contact@fanhive.jp` |
| 規約への同意 | 投稿画面に追加（今回） |
| 自分の投稿の削除 | できる（`deleteEvent`） |

### その他

- **3.1.1**: 外部の購入リンクは楽天市場・Yahoo!ショッピングの**物販**のみ。
  デジタルコンテンツはアプリ内課金だけで売っている
- **4.8**: サードパーティ/ソーシャルログインを使っていない（メールのワンタイムコードのみ）
  ので Sign in with Apple は不要
- **2.5.2**: iOS は `dist` を同梱。リモートのコードを実行していない
- **広告**: 本番のAdMob ID（`ca-app-pub-3561970163550872/...`）、`isTesting: false`、
  Googleのテスト用ID(`3940256099942544`)の混入なし。ATTは広告の前に出している
- **管理画面・デバッグ用のルートは無い**（`App.tsx` のRoute一覧を確認）

## 3. 残っているリスク

### 中: 投稿前の自動フィルタが無い（1.2）

1.2 は「不適切な素材が投稿されるのを防ぐ手段」を求める。今は**通報・ブロック・24時間以内の
手動削除**で運用していて、投稿時の自動チェックが無い。

ただし 5回の審査で 1.2 は一度も指摘されていない。8/15の情報要求への回答で通報・ブロックの
仕組みを説明し、審査はそのまま進んだ。
NGワード判定を入れる手もあるが、**誤検知で審査員の投稿が弾かれるほうが害が大きい**ので、
今回は入れない判断。指摘されてから足すほうが安全。

### 低: 第三者の権利物（5.2.1）

作品名と商品画像を扱う。画像・価格・リンクは楽天/Yahoo!ショッピングのアフィリエイトAPI由来で
規約上許諾されており、作品名はユーザーがフォローする**テキストのラベル**としてのみ出る。
8/15の回答でこの内容を説明済み。

## 4. 本人が App Store Connect で見るしかないもの

1. **スクリーンショットに「写真から読み取る」が写っていないか。**
   写っていたら 2.3.3（スクリーンショットはアプリの実際の使用を示すこと）で落ちる。
   投稿画面のスクショを使っているなら撮り直す
2. **年齢レーティング**。ユーザー投稿があるので、アンケートで
   「ユーザー生成コンテンツ」を申告していること（4+のままだと指摘され得る）
3. **サポートURL・マーケティングURL**が生きていること（404は 1.5 で落ちる）
4. **利用規約(EULA)** と **プライバシーポリシーURL** のフィールドが埋まっていること
   （最初の却下がここだった）
5. **定期購読ごとの審査用スクリーンショット**が登録されていること
6. **App Privacy** の申告内容が実態と合っていること（メール・ユーザーコンテンツ・
   広告用の識別子）。今回の変更で増減は無い
