diff --git a/node_modules/@ebarooni/capacitor-calendar/android/src/main/java/dev/barooni/capacitor/calendar/implementation/CapacitorCalendar.kt b/node_modules/@ebarooni/capacitor-calendar/android/src/main/java/dev/barooni/capacitor/calendar/implementation/CapacitorCalendar.kt
index 1b0008b..4ac1f9e 100644
--- a/node_modules/@ebarooni/capacitor-calendar/android/src/main/java/dev/barooni/capacitor/calendar/implementation/CapacitorCalendar.kt
+++ b/node_modules/@ebarooni/capacitor-calendar/android/src/main/java/dev/barooni/capacitor/calendar/implementation/CapacitorCalendar.kt
@@ -99,7 +99,15 @@ class CapacitorCalendar(
                 input.availability?.let { put(CalendarContract.Events.AVAILABILITY, it) }
                 input.organizer?.let { put(CalendarContract.Events.ORGANIZER, it) }
                 input.duration?.let { put(CalendarContract.Events.DURATION, it) }
-                input.color?.let { put(CalendarContract.Events.EVENT_COLOR, it) }
+                // FanHive パッチ: 色はアカウントの色表のindexで書く（生の色は表示に使われない）
+                input.color?.let {
+                    val key = ImplementationHelper.nearestEventColorKey(cr, calendarId, it)
+                    if (key != null) {
+                        put(CalendarContract.Events.EVENT_COLOR_KEY, key)
+                    } else {
+                        put(CalendarContract.Events.EVENT_COLOR, it)
+                    }
+                }
                 input.recurrence?.let { put(CalendarContract.Events.RRULE, it.toRRule()) }
             }
         val uri: Uri? = cr.insert(CalendarContract.Events.CONTENT_URI, values)
@@ -111,6 +119,8 @@ class CapacitorCalendar(
 
     fun modifyEvent(input: ModifyEvent) {
         val cr = plugin.context.contentResolver
+        // FanHive パッチ: 色のindexを引くのに、その予定が乗っているカレンダーが要る
+        val colorCalendarId: Long? = input.calendarId ?: ImplementationHelper.getEventCalendarId(cr, input.id)
         val values =
             ContentValues().apply {
                 input.title?.let { put(CalendarContract.Events.TITLE, it) }
@@ -119,10 +129,18 @@ class CapacitorCalendar(
                 input.startDate?.let { put(CalendarContract.Events.DTSTART, it) }
                 input.endDate?.let { put(CalendarContract.Events.DTEND, it) }
                 input.isAllDay?.let { put(CalendarContract.Events.ALL_DAY, it) }
+                input.timezoneId?.let { put(CalendarContract.Events.EVENT_TIMEZONE, it) }
                 input.description?.let { put(CalendarContract.Events.DESCRIPTION, it) }
                 input.availability?.let { put(CalendarContract.Events.AVAILABILITY, it) }
                 input.organizer?.let { put(CalendarContract.Events.ORGANIZER, it) }
-                input.color?.let { put(CalendarContract.Events.EVENT_COLOR, it) }
+                input.color?.let { color ->
+                    val key = colorCalendarId?.let { ImplementationHelper.nearestEventColorKey(cr, it, color) }
+                    if (key != null) {
+                        put(CalendarContract.Events.EVENT_COLOR_KEY, key)
+                    } else {
+                        put(CalendarContract.Events.EVENT_COLOR, color)
+                    }
+                }
                 input.duration?.let { put(CalendarContract.Events.DURATION, it) }
                 input.recurrence?.let { put(CalendarContract.Events.RRULE, it.toRRule()) }
             }
diff --git a/node_modules/@ebarooni/capacitor-calendar/android/src/main/java/dev/barooni/capacitor/calendar/models/inputs/CreateEventInput.kt b/node_modules/@ebarooni/capacitor-calendar/android/src/main/java/dev/barooni/capacitor/calendar/models/inputs/CreateEventInput.kt
index 71e90bf..295b896 100644
--- a/node_modules/@ebarooni/capacitor-calendar/android/src/main/java/dev/barooni/capacitor/calendar/models/inputs/CreateEventInput.kt
+++ b/node_modules/@ebarooni/capacitor-calendar/android/src/main/java/dev/barooni/capacitor/calendar/models/inputs/CreateEventInput.kt
@@ -23,6 +23,8 @@ data class CreateEventInput(
     val duration: String? = call.getString("duration")
     val color: Int? = ImplementationHelper.hexToColorInt(call.getString("color"))
     val attendees: List<EventGuest>? = ImplementationHelper.eventGuestsFromCall(call)
-    val timezoneId = TimeZone.getDefault().id
+    // 全日予定は CalendarContract の決まりで UTC 0時 + EVENT_TIMEZONE=UTC でないと
+    // 読み手（Googleカレンダー等）によって前日にずれる。
+    val timezoneId = if (isAllDay == 1) "UTC" else TimeZone.getDefault().id
     val recurrence = input.recurrence
 }
diff --git a/node_modules/@ebarooni/capacitor-calendar/android/src/main/java/dev/barooni/capacitor/calendar/models/inputs/ModifyEvent.kt b/node_modules/@ebarooni/capacitor-calendar/android/src/main/java/dev/barooni/capacitor/calendar/models/inputs/ModifyEvent.kt
index 6d792da..3f36144 100644
--- a/node_modules/@ebarooni/capacitor-calendar/android/src/main/java/dev/barooni/capacitor/calendar/models/inputs/ModifyEvent.kt
+++ b/node_modules/@ebarooni/capacitor-calendar/android/src/main/java/dev/barooni/capacitor/calendar/models/inputs/ModifyEvent.kt
@@ -5,6 +5,7 @@ import dev.barooni.capacitor.calendar.PluginError
 import dev.barooni.capacitor.calendar.models.data.EventGuest
 import dev.barooni.capacitor.calendar.models.data.EventRecurrenceRule
 import dev.barooni.capacitor.calendar.utils.ImplementationHelper
+import java.util.TimeZone
 
 data class ModifyEvent(
     private val call: PluginCall,
@@ -16,6 +17,10 @@ data class ModifyEvent(
     val startDate: Long? = call.getLong("startDate")?.let { ImplementationHelper.getCalendarFromTimestamp(it).timeInMillis }
     val endDate: Long? = call.getLong("endDate")?.let { ImplementationHelper.getCalendarFromTimestamp(it).timeInMillis }
     val isAllDay: Int? = call.getBoolean("isAllDay")?.let { if (it) 1 else 0 }
+
+    // 全日⇄時刻ありに変わったとき、作成時に入れた EVENT_TIMEZONE が残ると日付・時刻がずれる。
+    // 全日は UTC、時刻ありは端末TZ。
+    val timezoneId: String? = isAllDay?.let { if (it == 1) "UTC" else TimeZone.getDefault().id }
     val alerts: List<Int>? = ImplementationHelper.jsArrayToIntArray(call.getArray("alerts"))
     val description: String? = call.getString("description")
     val availability: Int? = call.getInt("availability")
diff --git a/node_modules/@ebarooni/capacitor-calendar/android/src/main/java/dev/barooni/capacitor/calendar/utils/ImplementationHelper.kt b/node_modules/@ebarooni/capacitor-calendar/android/src/main/java/dev/barooni/capacitor/calendar/utils/ImplementationHelper.kt
index 24e7144..85317bd 100644
--- a/node_modules/@ebarooni/capacitor-calendar/android/src/main/java/dev/barooni/capacitor/calendar/utils/ImplementationHelper.kt
+++ b/node_modules/@ebarooni/capacitor-calendar/android/src/main/java/dev/barooni/capacitor/calendar/utils/ImplementationHelper.kt
@@ -14,6 +14,83 @@ import java.util.Calendar
 
 class ImplementationHelper {
     companion object {
+        // ── FanHive パッチ ───────────────────────────────────────────────
+        // Googleカレンダーは予定の色を **Colors表のindex（EVENT_COLOR_KEY）でしか表示しない**。
+        // 生の EVENT_COLOR を書いてもプロバイダには入るが表示には使われない（2026-08-09 実機で確認）。
+        // そこでアカウントが持つ「予定の色」の中から一番近いものを選び、そのindexを書く。
+
+        private fun colorDistance(
+            a: Int,
+            b: Int,
+        ): Int {
+            val dr = ((a shr 16) and 0xFF) - ((b shr 16) and 0xFF)
+            val dg = ((a shr 8) and 0xFF) - ((b shr 8) and 0xFF)
+            val db = (a and 0xFF) - (b and 0xFF)
+            return dr * dr + dg * dg + db * db
+        }
+
+        /** 予定が乗るカレンダーのアカウントが持つ色のうち、`color` に一番近いもののindex。
+         *  色を持たないアカウント（端末ローカル等）では null。 */
+        fun nearestEventColorKey(
+            cr: ContentResolver,
+            calendarId: Long,
+            color: Int,
+        ): String? {
+            var accountName: String? = null
+            var accountType: String? = null
+            cr.query(
+                ContentUris.withAppendedId(CalendarContract.Calendars.CONTENT_URI, calendarId),
+                arrayOf(CalendarContract.Calendars.ACCOUNT_NAME, CalendarContract.Calendars.ACCOUNT_TYPE),
+                null,
+                null,
+                null,
+            )?.use {
+                if (it.moveToFirst()) {
+                    accountName = it.getString(0)
+                    accountType = it.getString(1)
+                }
+            }
+            val name = accountName ?: return null
+            val type = accountType ?: return null
+
+            var bestKey: String? = null
+            var bestDist = Int.MAX_VALUE
+            cr.query(
+                CalendarContract.Colors.CONTENT_URI,
+                arrayOf(CalendarContract.Colors.COLOR_KEY, CalendarContract.Colors.COLOR),
+                "${CalendarContract.Colors.ACCOUNT_NAME}=? AND ${CalendarContract.Colors.ACCOUNT_TYPE}=? AND " +
+                    "${CalendarContract.Colors.COLOR_TYPE}=?",
+                arrayOf(name, type, CalendarContract.Colors.TYPE_EVENT.toString()),
+                null,
+            )?.use { c ->
+                while (c.moveToNext()) {
+                    val key = c.getString(0) ?: continue
+                    val d = colorDistance(color, c.getInt(1))
+                    if (d < bestDist) {
+                        bestDist = d
+                        bestKey = key
+                    }
+                }
+            }
+            return bestKey
+        }
+
+        /** 既存の予定が今どのカレンダーに乗っているか（modifyEvent で calendarId 省略時に使う）。 */
+        fun getEventCalendarId(
+            cr: ContentResolver,
+            eventId: Long,
+        ): Long? {
+            cr.query(
+                ContentUris.withAppendedId(CalendarContract.Events.CONTENT_URI, eventId),
+                arrayOf(CalendarContract.Events.CALENDAR_ID),
+                null,
+                null,
+                null,
+            )?.use { if (it.moveToFirst()) return it.getLong(0) }
+            return null
+        }
+        // ── ここまで FanHive パッチ ──────────────────────────────────────
+
         fun getCalendarFromTimestamp(timestamp: Long?): Calendar =
             Calendar.getInstance().apply {
                 timeInMillis = timestamp ?: System.currentTimeMillis()
